# 開発環境構築ガイド（初心者向け・Windows／Mac／ChromeOS対応）

> 対象読者：CLI（コマンドライン）操作にまだ慣れていない人。
> ゴール：Java・JavaScript・PHPの開発ができる状態、およびLAMP環境（Apache／MySQL／PHP）の基礎を、使っているOSに関わらず自力で構築できるようにする。

---

## 0. 最初に：PATHとシェルの話（全OS共通の前提知識）

つまずきの9割はここの理解不足から来るので、各OSの手順に入る前に必ず読んでください。

### 0.1 シェルとは

「ターミナル」で文字を打つと、それを解釈して実行するプログラムが裏で動いています。これが**シェル**です。OSごとに標準のシェルが違います。

| OS | 標準のシェル | 備考 |
|---|---|---|
| Windows | PowerShell（新しめ）／コマンドプロンプト（古い） | Git Bashを別途入れるとLinux風のシェルも使える |
| Mac | zsh（macOS Catalina以降） | それより前はbashが標準だった |
| Linux／ChromeOS(Linux) | bash（ディストリビューションによる） | |

**同じコマンドでも、シェルが違うと書き方が変わることがあります。** 手順をコピペするときは「これはどのシェル向けか」を必ず確認してください。

### 0.2 PATHとは

`java`や`node`とコマンドを打ったとき、OSはそのプログラムの実体（`.exe`ファイルなど）がどこにあるかを知りません。**PATH（環境変数）は「実行ファイルを探す場所のリスト」**です。ソフトをインストールしても、そのインストール先がPATHに登録されていなければ「コマンドが見つかりません」というエラーになります。

つまずきパターンは決まっていて、ほぼ以下のどれかです。

1. インストールはできたが、PATHに追加していない
2. PATHに追加したが、**ターミナル（またはIDE）を再起動していない**ので反映されていない
3. PATHに追加する場所を間違えた（ユーザー環境変数とシステム環境変数を混同、`.bashrc`ではなく`.bash_profile`に書いた等）
4. 複数バージョンをインストールして、PATHの中で古い方が優先されている

### 0.3 「インストールできたか」の確認方法（全OS共通）

新しいターミナルを開いて、以下を打ってバージョンが表示されればOKです。**表示されない＝PATHが通っていないか、インストール自体に失敗しています。**

```bash
java -version
node -v
npm -v
php -v
git --version
```

---

## 1. Windows編

### 1.1 CLIの種類とどれを使うべきか

Windowsには複数のCLIがあり、初心者はここで混乱しがちです。

| 種類 | 特徴 | おすすめ度 |
|---|---|---|
| コマンドプロンプト（cmd） | 一番古い。今から新規に使う理由は薄い | △ |
| PowerShell | Windows標準の新しいシェル。`.ps1`スクリプトが動く | ◎（Windows操作全般） |
| Git Bash | Gitと一緒に入る、Linux風のシェル | ◎（Linux風コマンドを使いたい時） |
| WSL（Windows Subsystem for Linux） | Windows上で本物のLinuxが動く | ◎（LAMPを本格的にやりたいなら一番おすすめ） |

**方針**：Windows操作・Tomcat起動などは**PowerShell**、Linux風のコマンド（`find`や`grep`等）を使いたい時は**Git Bash**、と使い分けるのが実用的です。本気でLAMP環境を作るなら、後述の**WSL2**を強く推奨します（Windows上で無理にApache/MySQLを動かすより、Linuxをそのまま使った方がつまずきが少ないです）。

### 1.2 パッケージマネージャ：winget

Windows 10/11には`winget`が標準搭載されています（無ければMicrosoft Storeから「アプリ インストーラー」を更新）。

```powershell
winget --version
```

### 1.3 Git

```powershell
winget install --id Git.Git -e --source winget
```

インストール後、新しいターミナルで`git --version`を確認。

### 1.4 Java（JDK）

```powershell
winget install --id EclipseAdoptium.Temurin.21.JDK -e
```

（バージョン番号は必要に応じて変更。`winget search Temurin`で一覧を確認できます）

**PATHの通し方（Windows特有の躓きどころ）**：

多くのインストーラーは自動でPATHを通してくれますが、通っていない場合は手動設定が必要です。

1. 「システム環境変数の編集」を検索して開く
2. 「環境変数」ボタン→「システム環境変数」の`Path`を選択→編集
3. JDKのインストール先の`bin`フォルダ（例：`C:\Program Files\Eclipse Adoptium\jdk-21\bin`）を追加
4. 同様に`JAVA_HOME`という新しい変数を作り、値をJDKのインストール先（`bin`無し）にする

**⚠️ 最重要の躓きポイント**：設定を保存しても、**開いたままのターミナルには反映されません**。必ずターミナル（とIDE）を一度閉じて開き直してください。「さっき設定したのに反映されない」の9割はこれです。

### 1.5 Node.js（JavaScript）

```powershell
winget install OpenJS.NodeJS.LTS
```

バージョン管理をしたい場合（複数プロジェクトで違うNode.jsバージョンが必要になったら）は`nvm-windows`の導入を検討してください。

### 1.6 PHP

```powershell
winget install PHP.PHP.8.3
```

こちらもPATHが自動で通らないことがあるので、1.4と同じ要領で`php.exe`のあるフォルダをPathに追加してください。

### 1.7 LAMP相当の環境（Windows）

Windows上でLinux本来の「LAMP」（Linux/Apache/MySQL/PHP）をそのまま動かすことはできないので、選択肢は2つです。

**選択肢A：XAMPP（お手軽、ただし本番に近い挙動ではない）**

Apache・MySQL（MariaDB）・PHPがセットになったインストーラー。`https://www.apachefriends.org/`からダウンロードしてGUIでインストールするだけ。個別にPATHを通す必要はなく、XAMPP専用のコントロールパネルからApache/MySQLを起動・停止します。

**選択肢B：WSL2 + 本物のLinux（本気でやるならこちら）**

```powershell
wsl --install
```

再起動後、Ubuntu環境がセットアップされます。以降は本ガイドの**「4. Linux編」をそのままWSL2内で実行**すれば、Windows上でも本物のLAMP環境が手に入ります。EC-Liteのような実務に近い開発をするなら、こちらの方が最終的に躓きが少ないです。

### 1.8 Windows特有の詰まりどころまとめ

- **PATH変更後、ターミナル再起動を忘れる**（前述の最重要ポイント）
- **ユーザー環境変数とシステム環境変数の混同**：`setx VAR value`はユーザー環境変数、`setx VAR value /M`はシステム環境変数（要管理者権限）。IDEやサービスとして動くプロセスはシステム環境変数しか見ないことがある
- **パスにスペースが含まれる**（`C:\Program Files\...`）：コマンドラインで直接打つ場合はダブルクォートで囲む
- **Git Bashでの`/c/...`形式パスと、Windows形式`C:\...`パスの混在**：Git Bash特有の自動パス変換が、意図しない場所でURLやgcloud等のコマンドを誤変換することがある
- **PowerShellの実行ポリシー**：`.ps1`スクリプトが「このシステムではスクリプトの実行が無効になっています」と拒否されることがある。開発機なら`Set-ExecutionPolicy RemoteSigned -Scope CurrentUser`で緩和できる

---

## 2. Mac編

### 2.1 CLIはターミナル.app（デフォルトはzsh）

「ターミナル」アプリを開くだけです。macOS Catalina（2019年）以降はzshが標準シェルです。**古いネット記事の手順が`.bash_profile`を編集するよう書いてあっても、今のMacでは`.zshrc`を編集する必要がある**ので注意してください（これが最頻出の詰まりポイントです）。

### 2.2 Homebrew（パッケージマネージャ）

```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

インストール後、**画面の指示に必ず従ってPATHを通してください**。Apple Silicon（M1/M2/M3）とIntel Macでインストール先が違うため、PATHの通し方も変わります。

| Mac種別 | Homebrewのインストール先 | PATHに追加するコマンド |
|---|---|---|
| Apple Silicon（M1以降） | `/opt/homebrew` | `echo 'eval "$(/opt/homebrew/bin/brew shellenv)"' >> ~/.zshrc` |
| Intel | `/usr/local` | `echo 'eval "$(/usr/local/bin/brew shellenv)"' >> ~/.zshrc` |

編集後は`source ~/.zshrc`するか、ターミナルを開き直してください。

### 2.3 Git

Xcode Command Line Toolsに含まれています。

```bash
xcode-select --install
```

### 2.4 Java（JDK）

```bash
brew install openjdk@21
```

Homebrewでインストールしたjavaは、シンボリックリンクを作らないと`java`コマンドとして認識されないことが多いです。インストール後に表示される指示（`brew info openjdk@21`でも確認可）に従ってください。典型的には：

```bash
sudo ln -sfn /opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk /Library/Java/JavaVirtualMachines/openjdk-21.jdk
```

`JAVA_HOME`は`~/.zshrc`に以下を追加：

```bash
export JAVA_HOME=$(/usr/libexec/java_home -v21)
export PATH="$JAVA_HOME/bin:$PATH"
```

### 2.5 Node.js（JavaScript）

```bash
brew install node
```

複数バージョンを切り替えたい場合は`nvm`（Node Version Manager）の導入を推奨します。

### 2.6 PHP

```bash
brew install php
```

### 2.7 LAMP相当の環境（Mac）

Macは元々Apacheが内蔵されていますが（`httpd`）、バージョンが古く、システム領域を触るので事故りやすいです。**Homebrewで別途インストールする方が安全**です。

```bash
brew install httpd
brew install mysql
brew services start httpd
brew services start mysql
```

`brew services`はmacOS特有の便利コマンドで、OS起動時に自動起動する設定も含めてサービスを管理してくれます。GUIツールが良ければ**MAMP**（`https://www.mamp.info/`）も選択肢です。

### 2.8 Mac特有の詰まりどころまとめ

- **`.bash_profile`に書いたのに反映されない** → 今のMacはzshがデフォルトなので`.zshrc`に書く
- **Apple SiliconとIntelでHomebrewのパスが違う**（`/opt/homebrew` vs `/usr/local`）：ネットの古い記事は`/usr/local`前提で書かれていることが多いので注意
- **`sudo`が必要な操作とそうでない操作の混同**：Homebrew自体のインストール・パッケージインストールには基本`sudo`は不要（`sudo`を付けるとむしろ権限エラーになることがある）。ただしJavaのシンボリックリンク作成のようにシステム領域を触る操作は`sudo`が必要
- **Gatekeeper（セキュリティ機能）に開発元不明として実行をブロックされる**：「システム設定」→「プライバシーとセキュリティ」から個別に許可が必要な場合がある

---

## 3. ChromeOS編

ChromeOSは「CLI」という言葉が指すものが2種類あるので、まずそこを区別してください。

### 3.1 crosh（ChromeOSの標準ターミナル）— 開発用ではない

`Ctrl + Alt + T`で開ける、ChromeOS標準のターミナルです。**これは開発用のシェルではありません。** ネットワーク診断や一部のシステム設定変更のためのもので、`ping`や`ssh`程度のコマンドしか使えず、Java/PHP/Node.jsのインストールはできません。**ここで詰まっている人が非常に多い**（croshを本物のLinuxシェルだと勘違いする）ので、最初に区別しておきます。

### 3.2 Linux（Crostini）を有効化する — 実際の開発はここで行う

1. 「設定」を開く
2. 左メニューの「詳細設定」→「デベロッパー」（Developers）
3. 「Linux開発環境」の項目で「オン」にする
4. 数分待つとDebianベースのLinux環境（Crostini）がセットアップされ、専用のターミナルアプリが追加される

ここから先は、**専用のLinuxターミナルアプリ**を開いて操作します（croshではありません）。中身はDebian系Linuxなので、**このガイドの「4. Linux編」がそのまま使えます。**

### 3.3 ChromeOS特有の詰まりどころ

- **croshとLinuxターミナルを混同する**：前述の通り、開発は必ず「Linux」アプリの方で行う
- **ファイルシステムが分離されている**：ChromeOS本体の「ダウンロード」フォルダと、Linux環境内のホームディレクトリは別物。ファイルを共有したい場合は「Linux ファイル」というフォルダ経由でアクセスする（Files appに表示される）
- **マシンスペックの制約**：Chromebookは低スペック機が多く、Linux環境がRAM/ストレージを大きく消費すると本体側が重くなることがある
- **一部のChromebookはLinux機能自体に対応していない**（特に古い/廉価モデル）。「設定」に「デベロッパー」項目自体が出ない場合は非対応

---

## 4. Linux編（ChromeOS Linuxモード／WSL2でも共通）

Ubuntu／Debian系を前提とします（ChromeOSのLinux開発環境、WSL2のUbuntuも同じ手順で通ります）。

### 4.1 まずアップデート

```bash
sudo apt update && sudo apt upgrade -y
```

**これを忘れると、後続のインストールでパッケージが見つからない・古いバージョンが入る、という事態になりがちです。**

### 4.2 Git

```bash
sudo apt install git -y
```

### 4.3 Java（JDK）

```bash
sudo apt install openjdk-21-jdk -y
```

`JAVA_HOME`は通常自動設定されますが、されない場合は`~/.bashrc`に追記：

```bash
export JAVA_HOME=$(dirname $(dirname $(readlink -f $(which javac))))
export PATH="$JAVA_HOME/bin:$PATH"
```

編集後：

```bash
source ~/.bashrc
```

### 4.4 Node.js（JavaScript）

apt標準リポジトリのNode.jsは古いことが多いので、NodeSourceの公式スクリプト経由を推奨します。

```bash
curl -fsSL https://deb.nodesource.com/setup_lts.x | sudo -E bash -
sudo apt install nodejs -y
```

### 4.5 PHP

```bash
sudo apt install php php-cli php-mysql -y
```

### 4.6 LAMP一式（Apache／MySQL／PHP）

```bash
sudo apt install apache2 mysql-server php libapache2-mod-php php-mysql -y

sudo service apache2 start
sudo service mysql start

# MySQLの初期設定（root パスワード等）
sudo mysql_secure_installation
```

動作確認：ブラウザで`http://localhost/`にアクセスし、Apacheのデフォルトページが出ればOK。

### 4.7 Linux特有の詰まりどころ

- **`.bashrc`と`.bash_profile`/`.profile`の違い**：`.bashrc`は対話シェル起動のたびに読まれるが、`.bash_profile`はログインシェルの時だけ。ターミナルアプリの実装によってどちらが読まれるか変わることがあるので、両方に同じ内容を書くか、`.bash_profile`から`.bashrc`を読み込ませる設定にしておくと安全
- **`sudo`忘れ／付けすぎ**：システム領域へのインストールは`sudo`必須だが、個人の設定ファイル編集に`sudo`を使うと所有者が`root`になってしまい、後で「Permission denied」に悩まされる
- **WSL2の場合、Windows側とLinux側でファイルパスの扱いが違う**：`/mnt/c/...`でWindowsのCドライブにアクセスできるが、パフォーマンスが落ちる。開発ファイルはLinux側のホームディレクトリに置くのが基本
- **サービスの自動起動設定を忘れる**：`sudo service apache2 start`は今回だけの起動。再起動後も自動で立ち上げたい場合は`sudo systemctl enable apache2`（systemd環境の場合。WSL2はデフォルトでsystemdが無効なことがある）

---

## 5. LAMPとは何か（前提知識の補足）

**L**inux／**A**pache／**M**ySQL／**P**HPの頭文字。「Webサーバー（Apache）＋データベース（MySQL）＋サーバーサイド言語（PHP）が、Linux上で動く」という、Web開発の伝統的な組み合わせを指します。

- Windows上で同じ組み合わせを作ると通称**WAMP**、Mac上なら**MAMP**と呼ばれることがありますが、中身（Apache/MySQL/PHP）は同じです
- EC-Liteプロジェクト自体はLAMPではなく「Tomcat（Java）＋Neon（PostgreSQL）」という別の組み合わせですが、PHPやMySQLを使う別プロジェクトに触れる可能性を見越して、こちらも構築できるようにしておく、という位置付けです

### OS別・LAMP構成要素の対応表

| 要素 | Linux | Windows | Mac |
|---|---|---|---|
| Webサーバー | Apache（`apt install apache2`） | XAMPP同梱 または IIS | Apache（`brew install httpd`） |
| DB | MySQL/MariaDB（`apt install mysql-server`） | XAMPP同梱 | `brew install mysql` |
| 言語 | PHP（`apt install php`） | XAMPP同梱 | `brew install php` |

---

## 6. PATHの通し方 総まとめ（OS横断）

| OS | 設定ファイル／場所 | 反映方法 |
|---|---|---|
| Windows | システムのプロパティ→環境変数 | ターミナル・IDEを**再起動** |
| Mac（zsh） | `~/.zshrc` | `source ~/.zshrc` またはターミナル再起動 |
| Linux／ChromeOS Linux | `~/.bashrc` | `source ~/.bashrc` またはターミナル再起動 |

**共通の鉄則**：PATHを設定ファイルに書いただけでは反映されません。**必ず読み込ませる操作（`source`）か、ターミナルの再起動が必要**です。IDE経由でターミナルを使っている場合は、IDE自体の再起動が必要なこともあります。

---

## 7. インストール後の動作確認チェックリスト

新しいターミナルを開いて、上から順に実行してください。全部バージョンが表示されれば環境構築は完了です。

```bash
git --version
java -version
javac -version
node -v
npm -v
php -v
```

LAMPまで構築した場合は追加で：

```bash
apache2 -v          # Windowsの場合はXAMPPコントロールパネルで確認
mysql --version
```

---

## 8. よくあるエラーと対処（OS共通）

| エラー | 原因 | 対処 |
|---|---|---|
| `command not found` / `'xxx' は、内部コマンドまたは外部コマンド...として認識されていません` | PATHが通っていない、またはターミナル未再起動 | §6を確認。設定後は必ずターミナルを開き直す |
| `Permission denied` | 実行権限が無い、または所有者が`root`になっている | Linux/Macで`sudo`の使いどころを見直す（§4.7・§2.8） |
| バージョンが古いまま変わらない | 複数バージョンがインストールされていて、古い方がPATHの優先順位で先に来ている | `which java`（Mac/Linux）や`where java`（Windows）で実際に使われているパスを確認し、PATHの順序を見直す |
| `UnsupportedClassVersionError`（Java） | コンパイル時のJDKバージョンと実行時のJDKバージョンが不一致 | コンパイル時に`--release`オプションで実行環境のバージョンを明示する |

---

## 9. このプロジェクト（EC-Lite）で実際に必要なものだけの最短パス

全部読むのが大変な場合のショートカットです。EC-Lite開発に必要なのは以下だけです。

1. JDK 25（`--release 24`でコンパイルするため、25系を入れる）＋ PATH／`JAVA_HOME`
2. Git（任意。プロジェクトはGit不使用だが、他ツールのインストールに必要な場合がある）
3. Apache Tomcat 10.1系（Portable版で可、LAMPのApacheとは別物なので混同しないこと）
4. Google Cloud SDK（`gcloud`コマンド、Cloud Runデプロイ用）

PHP・Node.js・LAMP一式はEC-Lite自体には不要です（将来的に他のWeb系プロジェクトに触る場合の準備として、本ガイドの該当セクションを参照してください）。

---

*開発環境構築ガイド（初心者向け） — 初版*
